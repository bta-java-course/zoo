/*
package org.btarikool.javacourse;

import java.awt.*;

public class WebShop {

    Label fLabel = new Label("Поле 1:"); // надпись
pane.add(fLabel, 0, 0); // добавляем надпись к области
    // позиция: столбец 0, строка 0
    TextField titleInput = new TextField(""); // поле ввода текста
pane.add(titleInput, 1, 0); // добавляем поле к области
    // позиция: столбец 1, строка 0
    Button button = new Button("Отправить");// Кнопка подтверждения
button.setPrefHeight(60); // высокая кнопка
pane.add(button, 2, 0, 1, 2);

    GridPane pane = new GridPane(); // создаем область с абс.
// расположением
pane.setPadding(new Insets(20)); // устанавливаем
// внутренние отступы
// pane.setGridLinesVisible(true); - показ линий сетки
pane.setHgap(25); // внешние горизонтальные
pane.setVgap(15);



public void start(Stage stage) {
        Pane pane = new Pane(); // создаем область абс. расположением
        Label fLabel = new Label("first:"); // надпись для поля ввода текста
        pane.getChildren().add(fLabel); // добавляем надпись к области
        TextField titleInput = new TextField(""); // создаем поле ввода текста
        pane.getChildren().add(titleInput); // добавляем его к области
        Scene scene = new Scene(pane, 800, 600); // создаем эпизод с нашей областью
        stage.setScene(scene); // добавляем его к сцене
        stage.setTitle("JavaFX Test");
        stage.show(); // показываем сцену
    }

}
*/
